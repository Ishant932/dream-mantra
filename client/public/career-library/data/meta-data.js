// Dream Mantra Career Library — Subjects, Streams metadata
const SUBJ_MAP = (typeof D !== 'undefined' && D.subjectMap) || {};
const STREAM_MAP = (typeof D !== 'undefined' && D.streamMap) || {};
const DEMAND_ORDER = ['Very High', 'High', 'Medium', 'Low'];
const PAGE = 60;
